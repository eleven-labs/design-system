import { Meta } from '@storybook/addon-docs';
import { Layout, SystemPropsTable } from '@storybook-components';
import { hiddenSystemProps } from '@/constants/systemProps';
import { Text } from '@/components';

<Meta title="Foundations/System Props/Hidden System Props" />

<Layout
    title="System Props"
    description="All properties available on Hidden System Props"
>
    <Text mb="s">
        Hide component if the media query matches.<br />
        The above is similar to "min-width" and below props is similar to "max-width".<br />
        They receive the breakpoint keys.
    </Text>
    <SystemPropsTable
        title="Hidden System Props"
        systemProps={hiddenSystemProps}
    />
</Layout>
