import { Meta } from '@storybook/addon-docs/blocks';
import { Layout, Table } from '@/storybook/components';
import { tokenVariables } from '@/tokens';

<Meta
  title="Foundations/Design Tokens/Color"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      disable: true,
      grid: {
        disable: true,
      }
    },
  }}
/>

<Layout
  title="Color"
  description="Color palettes used by both design and engineering"
>
  <Table
    columns={[
      {
        name: 'name',
        label: 'Name',
      },
      {
        name: 'preview',
        label: 'Preview',
      },
      {
        name: 'value',
        label: 'Value',
      },
      {
        name: 'usage',
        label: 'Usage',
      },
    ]}
    rows={Object.entries(tokenVariables.color).map(([tokenName, token]) => ({
      name: (
        <span className="font-medium">
          {tokenName}
        </span>
      ),
      preview: (
        <div
          style={{
            backgroundColor: token?.value,
            boxSizing: 'border-box',
            width: '200px',
            height: '50px',
            border: '1px solid'
          }}
        />
      ),
      value: token?.value,
      usage: token?.description,
    }))}
  />
</Layout>
