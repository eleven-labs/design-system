import { Meta } from '@storybook/addon-docs';
import { Layout, DesignTokenTable } from '@storybook-components';
import { cssProperties } from '@/constants';

<Meta
    title="Foundations/Css Properties/Layout/Display"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="CSS Properties"
    description="Display CSS property scales"
>
    <DesignTokenTable
        title="Display"
        tokenVariables={cssProperties['display']}
    />
</Layout>
