import { Meta } from '@storybook/addon-docs';
import { Layout, Table } from '@storybook-components';
import { Heading, Text } from '@/components';
import { tokenVariables, headingSizeTokenNameList, textSizeTokenNameList } from '@/constants';
import { getValueOfCssPropertyInDesignTokens } from '@/helpers/storybookHelper';

<Meta
    title="Foundations/Design Tokens/Typography"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Typography design guidelines and token scales"
>
    <Table
        title="Heading"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            },
            {
                name: 'mobile',
                label: 'Value for mobile',
            },
            {
                name: 'desktop',
                label: 'Value for desktop',
            },
        ]}
        rows={headingSizeTokenNameList.map(tokenName => ({
            name: (
                <Text weight="semi-bold">heading-{tokenName}</Text>
            ),
            preview: (
                <Heading size={tokenName}>
                    Title
                </Heading>
            ),
            mobile: `${getValueOfCssPropertyInDesignTokens({
                path: 'typography.heading',
                tokenName,
                device: 'mobile',
                propertyCSS: 'font-size'
            })} / ${getValueOfCssPropertyInDesignTokens({
              path: 'typography.heading',
              tokenName,
              device: 'mobile',
              propertyCSS: 'line-height'
            })}`,
            desktop: `${getValueOfCssPropertyInDesignTokens({
                path: 'typography.heading',
                tokenName,
                device: 'desktop',
                propertyCSS: 'font-size'
            })} / ${getValueOfCssPropertyInDesignTokens({
              path: 'typography.heading',
              tokenName,
              device: 'desktop',
              propertyCSS: 'line-height'
            })}`,
        }))}
    />
    <Table
        title="Text"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            },
            {
                name: 'mobile',
                label: 'Value for mobile',
            },
            {
                name: 'desktop',
                label: 'Value for desktop',
            },
        ]}
        rows={textSizeTokenNameList.map((tokenName) => ({
            name: (
                <Text weight="semi-bold">text-{tokenName}</Text>
            ),
            preview: (
                <Text size={tokenName}>
                   Lorem ipsum dolor sit amet, consectetur adipiscing elit
                </Text>
            ),
            mobile: `${getValueOfCssPropertyInDesignTokens({
                path: 'typography.text',
                tokenName,
                device: 'mobile',
                propertyCSS: 'font-size'
            })} / ${getValueOfCssPropertyInDesignTokens({
              path: 'typography.text',
              tokenName,
              device: 'mobile',
              propertyCSS: 'line-height'
            })}`,
            desktop: `${getValueOfCssPropertyInDesignTokens({
                path: 'typography.text',
                tokenName,
                device: 'desktop',
                propertyCSS: 'font-size'
            })} / ${getValueOfCssPropertyInDesignTokens({
              path: 'typography.text',
              tokenName,
              device: 'desktop',
              propertyCSS: 'line-height'
            })}`,
        }))}
    />
</Layout>
