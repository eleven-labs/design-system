import { Meta } from '@storybook/addon-docs/blocks';
import { Layout, Table } from '@/storybook/components';
import { tokenVariables, headingSizeTokenNameList, tokenVariablesDesktop } from '@/tokens';

<Meta
    title="Foundations/Design Tokens/Typography/Heading"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Heading token scales"
>
    <Table
        title="Heading"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            },
            {
              name: 'fontFamily',
              label: 'Font Family',
            },
            {
                name: 'mobile',
                label: 'Value for mobile',
            },
            {
                name: 'desktop',
                label: 'Value for desktop',
            },
        ]}
        rows={headingSizeTokenNameList
          .filter(tokenName => tokenName !== 'base')
          .map(tokenName => ({
            name: (
                <span className="font-medium">typography-heading-{tokenName}</span>
            ),
            preview: (
                <p className={`typography-heading-${tokenName}`}>
                    Title
                </p>
            ),
            fontFamily: tokenVariables.typography.heading.base['font-family'].value,
            mobile: `${tokenVariables.typography.heading[tokenName]['font-size']?.value || tokenVariables.typography.heading.base['font-size']?.value || '-'} / ${tokenVariables.typography.heading[tokenName]['line-height']?.value || tokenVariables.typography.heading.base['line-height']?.value || '-'}`,
            desktop: `${tokenVariablesDesktop.typography?.heading?.[tokenName]?.['font-size']?.value || tokenVariables.typography.heading[tokenName]['font-size']?.value || tokenVariables.typography.heading.base['font-size']?.value || '-'} / ${tokenVariablesDesktop.typography?.heading?.[tokenName]?.['line-height']?.value || tokenVariables.typography.heading[tokenName]['line-height']?.value || tokenVariables.typography.heading.base['line-height']?.value || '-'}`,
        }))}
    />
</Layout>
