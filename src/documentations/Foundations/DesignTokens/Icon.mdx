import { Meta } from '@storybook/addon-docs/blocks';
import { Layout, Table } from '@/storybook/components';
import { Icon } from '@/components';
import { iconTokenNameList } from '@/tokens';

<Meta
    title="Foundations/Design Tokens/Icon"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Icon"
    description="All available icons"
>
    <Table
        title="Common"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            }
        ]}
        rows={iconTokenNameList.map((iconName) => ({
          name: (
              <span className="font-medium">{iconName}</span>
          ),
          preview: <Icon name={iconName} width="5rem" height="5rem" color="navy" size="5rem" />,
        }))}
    />
</Layout>
