import { Meta } from '@storybook/addon-docs/blocks';
import { Layout, Table } from '@/storybook/components';
import { tokenVariables, textSizeTokenNameList, tokenVariablesDesktop } from '@/tokens';

<Meta
    title="Foundations/Design Tokens/Typography/Text"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Typography"
    description="Text token scales"
>
    <Table
        title="Text"
        columns={[
            {
                name: 'name',
                label: 'Name',
            },
            {
                name: 'preview',
                label: 'Preview',
            },
            {
              name: 'fontFamily',
              label: 'Font Family',
            },
            {
                name: 'mobile',
                label: 'Value for mobile',
            },
            {
                name: 'desktop',
                label: 'Value for desktop',
            },
        ]}
        rows={textSizeTokenNameList
          .filter(tokenName => tokenName !== 'base')
          .map((tokenName) => ({
            name: (
                <span className="font-medium">typography-text-{tokenName}</span>
            ),
            preview: (
                <p className={`typography-text-${tokenName}`}>
                   Lorem ipsum dolor sit amet, consectetur adipiscing elit
                </p>
            ),
            fontFamily: tokenVariables['font-family'].base.value,
            mobile: `${tokenVariables.typography.text[tokenName]['font-size']?.value || tokenVariables.typography.text.base['font-size']?.value || '-'} / ${tokenVariables.typography.text[tokenName]['line-height']?.value || tokenVariables.typography.text.base['line-height']?.value || '-'}`,
            desktop: `${tokenVariablesDesktop.typography?.text?.[tokenName]?.['font-size']?.value || tokenVariables.typography.text[tokenName]['font-size']?.value || tokenVariables.typography.text.base['font-size']?.value || '-'} / ${tokenVariablesDesktop.typography?.text?.[tokenName]?.['line-height']?.value || tokenVariables.typography.text[tokenName]['line-height']?.value || tokenVariables.typography.text.base['line-height']?.value || '-'}`,
        }))}
    />
</Layout>
