import { Meta } from '@storybook/addon-docs/blocks';
import { Layout, DesignTokenTable } from '@/storybook/components';
import { tokenVariables } from '@/tokens';

<Meta
    title="Foundations/Design Tokens/Layout/Height"
    parameters={{
        viewMode: 'docs',
        previewTabs: {
            canvas: { hidden: true }
        },
        backgrounds: {
            disable: true,
            grid: {
                disable: true,
            }
        },
    }}
/>

<Layout
    title="Layout"
    description="Height token scales"
>
    <DesignTokenTable
        title="Height"
        tokenVariables={tokenVariables['height']}
    />
</Layout>
